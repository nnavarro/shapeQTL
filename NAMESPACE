#exportPattern("^[[:alpha:]]+")
# Import dependencies
import(qtl, MASS, parallel)

# Export
export(scanoneShape)
export(update.cross)
export(get.geno)
export(fitqtl.shape)
export(effectsize.shape)
export(plot.shape)
